export interface SocialIconProps {
  src: string;
  alt: string;
}

export interface ContactItemProps {
  iconSrc: string;
  text: string;
}

export interface ActionIconProps {
  src: string;
  alt: string;
  linkTo?: string;
}